

Check if exit(exitcode) from system_calls.h works? 

Test if mDelay function are working.

Test if millis is working.

Test if #define PROGMEM __attribute__((section(".rodata"))) from motion.h is working + reading from the var.

